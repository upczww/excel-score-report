interface:
  display_name: "小题分报告生成器"
  short_description: "用内置 Node.js 工具离线生成逐生小题分 PDF 报告"
  default_prompt: "Use $excel-score-report to convert my local exam score workbook into an offline PDF with one student per page."

policy:
  allow_implicit_invocation: true

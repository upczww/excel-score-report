interface:
  display_name: "小题分报告生成器"
  short_description: "把考试小题分 Excel 工作簿生成逐生 PDF 成绩报告"
  default_prompt: "Use $excel-score-report to convert my exam item-score workbook into a paginated PDF with one student per page."

policy:
  allow_implicit_invocation: true
